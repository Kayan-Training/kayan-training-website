# SDD ledger — plan: docs/superpowers/plans/2026-09-14-event-pricing-contact-brochure.md

Spec: docs/superpowers/specs/2026-09-14-event-pricing-contact-brochure-design.md (reachable, read)

## Pre-flight conflict scan

| Pair / Task | Produces / Requires | Finding |
|---|---|---|
| Task 1 -> Task 2 | Task 1 produces `EventPriceTier{price:Decimal,currency,secondaryDisplayPrice,order}`, `EventContactNumber{number,labelEn,labelAr,order}`, `Event.brochureUrl`. Task 2 defines zod shapes `priceTierItemSchema`/`contactNumberItemSchema` with matching field names. | Consistent. |
| Task 2 -> Task 3/4/5 | Task 2 produces `priceTiers`/`contactNumbers`/`brochureUrl` on `EventFormValues`. Tasks 3/4/5 read/write these via `form.watch`/`form.setValue`. | Consistent. |
| Task 3/4 -> Task 6/7 | Field array item shapes (`titleEn`,`titleAr`,`descriptionEn`,`descriptionAr`,`price`,`currency`,`secondaryDisplayPrice`; `number`,`labelEn`,`labelAr`) match what `_actions.ts` tasks map into Prisma `create`/`createMany` calls. | Consistent. |
| Task 6/7 -> Task 8 | Tasks 6/7 write `EventPriceTier`+`EventPriceTierTranslation`(locale/title/description) and `EventContactNumber`. Task 8 reads them back via `include` and resolves with Trainer-style en-fallback for tiers, no-fallback for contact-number labels. | Consistent — resolution strategy differs per field type but each is explicit in the plan text, not accidental. |
| Task 8 -> Task 10/11/12/14 | Task 8 produces `EventDetail.priceTiers[]`, `.contactNumbers[]`, `.enquiriesNumbers[]`, `.brochureUrl`. Tasks 10/11/12 consume these exact shapes; Task 14 consumes `priceTiers` subset `{id,title,description,price,currency}`. | Consistent (Task 14's shape is a subset of Task 8's — fine, no extra fields required). |
| Task 8 settings fallback | Plan text: "reuse the existing settings lookup ... do not duplicate the settings-fetch logic". Exact export name from `src/lib/settings.ts` not given (placeholder-shaped instruction) — plan explicitly tells the implementer to look it up rather than guess. | Not a defect: this is a deliberate "confirm against real code" instruction, not an unresolved value. Implementer must grep `src/lib/settings.ts` + `footer.tsx` before writing this line. Ruling: acceptable as-is, flag to implementer explicitly in dispatch. |
| Task 9 -> Task 10/12/14 | Task 9 produces `<CurrencySymbol currency className? />`. Tasks 10/12/14 import and use it with `currency={tier.currency}` or `currency="OMR"`. | Consistent. |
| Task 9 asset step | Step 1 downloads a real external asset from cbo.gov.om at implementation time (network-dependent, may need a fallback if the page structure differs from research). | Ruling: if the presign/CDN page doesn't expose a direct SVG link at implementation time, implementer should note it in DONE_WITH_CONCERNS rather than block, and may hand-construct a faithful inline SVG of the described glyph (stylized ع/ر crossed by two lines) as a documented interim fallback, citing the source. Ledgered here so the reviewer doesn't treat a fallback SVG as scope creep. |
| Task 10 | Changes `priceLabel` from a string to JSX; plan explicitly calls out checking surrounding interpolation. | Consistent, flagged as an implementer checklist item already in the task text. |
| Task 13 | Plan's Step 1 explicitly says "no code change needed inside service.ts beyond widening the type" but also says "add priceTierId?: string | null to data ... if the Registration model has/gets a column for it — if not, skip". Global Constraint says no schema changes beyond Task 1's additive migration; Task 1 does NOT add a priceTierId column to Registration. | Ruling: do NOT add a priceTierId column (out of Task 1's migration, and no task adds one) — skip persisting the tier id itself, only use it to compute `amount`, exactly as the plan's fallback clause allows. This keeps Task 13 schema-neutral, consistent with Global Constraints. |
| Task 13/14/15 shared fn | Task 15 extracts `resolveTierAmount(priceTiers, flatPrice, submittedTierId)` and has Task 13's pages call it — but Task 13's own steps show the logic inlined. | Ruling: implement Task 13 with the logic inlined as its steps show (that's what Task 13's own diff/tests will cover informally); Task 15 then refactors to extract the pure function and adds the real unit tests. This is the plan's intended order (Task 15 runs after 13) — not a conflict, just sequencing. No action needed beyond executing in order. |
| Task 16 | References a possible separate brochure file vs `public/Opex 2026 - sponsor.pdf`; explicitly defers the Group-Rate-as-tier-or-not decision to product owner. | Not a defect — explicitly flagged in the plan as an open call to surface, per Self-Review Notes. Will surface to user at Task 16, not resolved here. |

Scan result: no blocking contradictions found. Two soft judgment calls ruled above (settings-lookup naming left to implementer's grep, CBO asset fallback allowed, no Registration schema column). Proceeding to Task 1.

Task 1: complete (commits 28f6bde..7c941f7, review clean — spec ✅, task quality approved, 3 minor notes not blocking: prisma/migrations gitignored so migration history isn't in git diff review [pre-existing repo convention, flagged transparently by implementer], no DB-level currency enum [matches existing stringly-typed schema style], no schema comment on secondaryDisplayPrice intent [cosmetic]).
Ruling: implementer's deviation from brief Step 2 (baseline-then-migrate instead of raw `migrate dev`) approved — raw migrate dev would have prompted a full destructive reset of the shared dev DB with real data, violating the additive-only global constraint. Baseline approach achieved zero data loss while producing a valid additive migration. Cost if wrong: none identified — reviewer independently verified the migration SQL on disk is purely additive.

Task 2: complete (commits 7c941f7..8e78cf1, review clean — spec ✅, task quality approved).
Ruling: implementer's deviation from the brief's literal zod syntax (plain `z.number()`/`z.string()` instead of `.coerce`/`.default()`/`.optional().default()`) approved — reviewer independently verified zod v4 + this project's `zodResolver` types the resolver's field-values generic to the schema's *input* type, so `.default()`/`.coerce` would make input≠output and break `useForm<EventFormValues>`'s type equality with real compile errors. Defaults are supplied via `initialFormValues` instead, functionally equivalent. Cost if wrong: none identified — reviewer ran `npx tsc --noEmit` independently and confirmed clean.
Ruling (plan defect found during Task 2 review, fixed before dispatch): the plan's admin edit route text said `dashboard/events/[id]`; the actual route is `dashboard/programs/[id]/page.tsx`, and that page builds `EventForm`'s `defaultValues` from its OWN separate `db.event.findUnique` query, not from Task 8's `queries.ts`. Task 7 (not yet dispatched) has been amended in-place to add a Step 0 wiring `priceTiers`/`contactNumbers`/`brochureUrl` into that page's fetch `include` and `defaultValues` object, and Task 7's Step 4/5 updated to verify round-tripping and commit the extra file. Tasks 3/4 manual-verification route references corrected to `programs/[id]`. This was load-bearing: without it, Task 7's delete-then-recreate logic would silently wipe any existing tiers/contact numbers on every save from the admin edit page. Cost if wrong: if this ruling is somehow mistaken (e.g. a second edit route exists elsewhere), Task 7's Step 4 manual verification will surface it — the round-trip check is explicit.

Task 3: complete (commits 8e78cf1..a2a43bc, review clean — spec ✅, task quality approved, 1 minor deferred).
Ruling: reviewer confirmed the pre-existing `pnpm next build` failure (`prismaAdapter` export from better-auth) is unrelated to this task's diff — independently verified the export resolves fine at plain Node/CommonJS level, so it's a Next.js/bundler resolution quirk, not a real missing export, and predates this plan. Not this task's concern; flagging for final-review awareness only in case it needs a real fix before this branch ships (out of this plan's scope otherwise).
Task 3: minor (deferred): Price Tier cards use `ghost-border` (a public-site marketing class) instead of this admin form's actual convention (`rounded-xl border border-zinc-200 bg-white p-4`, used by the adjacent Bank Transfer section) — brief specified `ghost-border` verbatim, implementer followed it faithfully, so this is a plan-text styling miss, not an implementation bug. Visually inconsistent with rest of admin form but non-blocking; final review should triage whether to restyle to match admin conventions before merge.
No manual browser verification performed for Task 3 (blocked by the unrelated pre-existing build issue) — ruled non-blocking for this task's acceptance since the code was traced by hand against the schema with no mismatches and the gate is bundler-level, not code-level. Real end-to-end browser verification is still owed before this branch ships — carrying forward to Task 16's manual verification pass (or earlier if the build issue gets resolved sooner).

Environment note (checked independently, not fixing — out of this plan's scope): `pnpm next build` fails project-wide on missing dependency `@better-auth/kysely-adapter` (better-auth's kysely adapter module does `export * from "@better-auth/kysely-adapter"` unconditionally; Turbopack statically resolves it even though the app only uses the Prisma adapter). Confirmed this pre-exists on the main checkout's package.json too (grepped `d:/web/kayan-training-nextjs/package.json` — same `better-auth: ^1.6.9`, no kysely-adapter dep) — not introduced by any task in this plan. This will block `pnpm dev`/`pnpm build`-based manual browser verification for every remaining task in this plan. Tasks will rely on `npx tsc --noEmit` + hand-traced logic for verification; a real browser-based end-to-end pass is deferred and must happen after someone resolves this dependency gap (add `@better-auth/kysely-adapter` as a devDependency, or otherwise) — flagging to surface in the final whole-branch review and to the user, not silently absorbing the gap.

Side note (user-flagged, off-plan, fixed directly against the shared dev DB, not part of this branch's diff): OPEX 2026's `EventTranslation.seoTitle`/`seoDescription` (both locales) still said Salalah/16-17 Aug — missed during the earlier same-session content-fix pass which only touched title/shortDescription/description. Fixed to Muscat/9-10 Nov 2026 for both locales, verified via direct query. Unrelated to this plan's code changes; noted here only for session continuity.

Task 4: complete (commits a2a43bc..b69b5e4, review clean — spec ✅, task quality approved, no issues found).
Task 5: complete (commits b69b5e4..5d1cb56, review clean — spec ✅, task quality approved, dedicated upload state confirmed not shared with cover-image uploader, no server-side scope creep, no issues found).
Task 6: complete (commits 5d1cb56..2db401d, review clean — spec ✅, task quality approved, real-DB round-trip verified by implementer and independently corroborated by reviewer against the shared Neon dev DB, no orphan rows, no issues found).
Task 7: complete (commits 2db401d..f4f4228, review clean — spec ✅ for both Part A [updateEventAction delete+recreate] and Part B [admin edit page defaultValues wiring, the amendment], task quality approved. Reviewer independently confirmed the load-bearing "save without touching tiers/numbers doesn't wipe them" regression test was real and correct, and confirmed opex-2026 remains untouched [0 tiers, 0 contact numbers, brochureUrl null — as expected pre-feature]. No issues found.).
Task 8: complete (commits f4f4228..ce81c26, review clean — spec ✅, task quality approved. Reviewer independently confirmed the two DIFFERENT fallback strategies [priceTiers: en/other-locale fallback; contactNumbers: no fallback] were correctly differentiated, confirmed the global-default settings lookup is the real `getLocalizedSiteSettings` export shared with footer/contact-us [no divergent second source of truth], and confirmed the diff is strictly additive to the shared live `getEventDetailBySlug` function.).
Task 8: minor (deferred): scratch verification only exercised the empty/fallback path against opex-2026 (0 tiers, 0 numbers) — the non-empty resolution path [en-fallback triggering, per-locale label omission] is unexercised by any test so far, though it's a low-risk map/find copied from an already-proven pattern. Carry forward to Task 16's end-to-end verification once real tier/contact data exists.
Task 9: complete (commits ce81c26..f501c7f, review clean — spec ✅, task quality approved). Reviewer independently WebFetched the real CBO page, independently rasterized the extracted SVG via sharp-cli and visually confirmed it's a genuine stylized glyph (not garbage/placeholder), independently verified the path's real bounding box tightly matches the declared viewBox, and independently confirmed the next/image usage against this fork's actual v16 docs. Real official asset, honestly attributed, no fallback needed.
Task 9: minor (deferred): SVG's internal role="img"/aria-label="OMR" is inert since next/image renders SVGs as a plain <img> (not inlined) — harmless dead markup, not blocking.
Task 10: complete (commits f501c7f..7dea5a3, review clean — spec ✅, task quality approved, both events/[slug] and training-courses/[slug] pages verified byte-for-byte identical). Implementer's judgment call [brochure download button unconditional relative to registration-open/closed/past state] independently reviewed and endorsed as correct UX reasoning.
Task 11: complete (commits 7dea5a3..2d28b1e, review clean — spec ✅, task quality approved, both files byte-for-byte identical, PhoneText/tel: href/optional-label all correct).
Task 11: minor (deferred): no manual browser verification of 0/1/2 contact-number scenarios (same pre-existing build-issue blocker as other UI tasks) — carried into Task 16's end-to-end pass.
Task 12: complete (commits 2d28b1e..e57834d, review clean — spec ✅, task quality approved, both files verified byte-for-byte identical component body, correct #pricing anchor match with Task 10, correct placement outside sidebar in all 4 layout branches [featured/regular x events/training-courses], no shadcn Card/Badge introduced).
Task 13: complete (commits e57834d..2289e56, review clean — spec ✅, SECURITY task quality approved). Adversarial review checked 5 explicit attack scenarios (client-submitted amount override, cross-event tier id, malformed/empty tier id, empty-tiers-array fallback, service.ts recomputation) — all SAFE with cited evidence. No critical/important findings. Ruling reaffirmed: no priceTierId column added to Registration (out of Task 1's migration scope) — implementer flagged the reporting-granularity tradeoff, accepted as-is per plan boundary.
Task 14: complete (commits 2289e56..c76e9a5, review clean — spec ✅, task quality approved). All 4 critical wiring points [name="priceTierId" exact match, native form-action not client-fetch, default pre-selection, zero-tiers renders nothing] independently verified safe against the actual file. The name="priceTierId"->formData.get("priceTierId") chain connecting to Task 13's already-security-reviewed logic confirmed intact and unbroken. Implementer's deviation from the brief's literal snippet (unconditional render inside <form> instead of nested under a step variable) reviewed and endorsed as a real bug-avoidance improvement.
Task 15: complete (commits c76e9a5..7a8f4f8, review clean — spec ✅, task quality approved). Reviewer independently ran `pnpm test:unit` and confirmed 4/4 passing (not just trusted the report), confirmed both register pages were updated in lockstep [no stale-inline-logic divergence], confirmed the vitest.config.ts alias deviation was necessary and correctly scoped [tsconfig @/* path mapping vs plain Vitest not reading tsconfig paths].
Task 15: minor (deferred): amount round-trips through Number()/String() in resolveTierAmount's call sites, which can reformat a Decimal.toString() like "380.00" to "380" versus Task 13's original pass-through. Numerically harmless (Registration.amount is a Prisma Decimal column, reparsed on write regardless of trailing-zero string formatting) — not fixing, noting for final review awareness only.
Ruling (user decision, off-plan question resolved before Task 16 dispatch): Group Rate stays as descriptive text only, no price-tier row for it. Brochure = public/Opex 2026 - sponsor.pdf (the same sponsor PDF already in the repo).
Environment note: `pnpm dev`/`pnpm build` blocked by pre-existing missing @better-auth/* packages (declared correctly in pnpm-lock.yaml, not materialized in node_modules — confirmed reproduced on main checkout too). Attempted `pnpm install --force`; failed partway on a Windows EPERM file-lock (likely another live `next dev` process on this shared machine holding a swc binary lock among ~18 running node.exe processes) — judged too risky to guess-kill processes on a shared machine. Not fixed. This blocks ALL browser-based verification for this entire plan's UI surface (Tasks 3-5, 10-12, 14, 16) — every such task substituted tsc/vitest/direct-DB/direct-query verification instead, consistently disclosed rather than hidden. Real browser QA of the rendered public pages and admin UI is still owed before this branch ships and should happen once a human resolves the node-process/install conflict on this machine.
Task 16: complete (commits: none — DB/S3 data only, no repo file changes). Reviewer independently re-queried the real DB/S3 and reproduced every claimed value exactly: 4 real price tiers (no Group Rate row) both locales, 2 real contact numbers, real brochure uploaded to S3 with byte-for-byte size match against the local PDF, pre-existing Group Rate prose confirmed present in both locale descriptions, clean git status, all tests/typecheck green. Reviewer judged the browser-verification substitution (direct calls to the real getEventDetailBySlug/resolveTierAmount functions against real production-shaped data) as reasonable and honest given the disclosed environment blocker, with the residual visual/rendering gap explicitly named rather than hidden.

## Plan status: all 16 tasks complete.

Final whole-branch review (opus, commits 28f6bde..7a8f4f8): Ready to merge = "With fixes". Found 1 Critical (currency glyph invisible on dark theme via next/image + currentColor not inheriting through <img>-loaded SVG — genuine bug, would have been caught instantly by the blocked browser QA), 3 Important (#2 registration confirmation screen showed flat price not selected tier's price; #3 brochure upload unreachable for free events; #4 migration files not in git, pre-existing repo convention now load-bearing for page availability — release-step concern, not a code defect, left untouched per instruction), 8 Minor (5 fixed: contact-number error mis-routing, glyph/price ordering inconsistency, key collision risk, no keyboard-focus indicator on tier radios; 4 explicitly deferred/accepted: admin styling inconsistency from Task 3, unused priceTierId param on createRegistration, non-transactional delete-then-recreate matching existing codebase convention, hardcoded "Delegate Enrollment Fees" heading on training-courses page).

Fix wave (commit 72a54d4): addressed Critical #1, Important #2 and #3, Minor #5/#6/#7/#10. Left untouched per instruction: #4 (release-step, not code), #8/#9/#11/#12 (deferred/accepted).

Scoped re-review of fix wave: all 7 addressed findings independently verified genuinely fixed (byte-for-byte SVG path match, correct JSX brace restructuring confirmed not just indentation, has-focus-visible confirmed valid in Tailwind v4.2.4) with no new breakage introduced. tsc clean, 4/4 tests passing.

Ruling: #4 (migration files gitignored, not committed) is accepted as a release-step concern to hand to the user, not a code fix — confirm the deploy target's DB has this plan's migration applied before/at merge time, since getEventDetailBySlug now queries relations (priceTiers/contactNumbers) that don't exist without it, which would 500 every event/training-course detail page, not just ones using the new feature.

RESIDUAL OPEN ITEM (real, not resolved by this plan): no actual browser-rendered verification exists anywhere in this plan's history — every task substituted tsc/vitest/direct-DB-query verification due to a pre-existing, confirmed-on-main @better-auth/* install gap plus a file-lock conflict during a repair attempt on this shared machine. The final review's own Critical finding (#1) was itself found by *reasoning about* the CSS/SVG semantics, not by seeing it — proof the gap has real cost even though every substitution was thorough and honest. A human should do one real page-load of /en/events/opex-2026, /ar/events/opex-2026, and one internal-registration flow through the tier picker, once the @better-auth install is fixed, before considering this fully done.
