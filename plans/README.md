# Implementation Plans

Generated by the improve skill on 2026-07-17. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Close four unauthenticated/unsafe security gaps | P1 | S | — | DONE (worktree `agent-a0797b451d55eebe8`, branch `advisor/001-security-quick-fixes`) |
| 002  | Add server-side admin auth checks to every dashboard server action | P1 | M | — | DONE (worktree `agent-aaa5f43d443ba5376`, branch `advisor/002-dashboard-action-auth`) |
| 003  | Make event registration capacity check and writes atomic | P1 | M | — | DONE (worktree `agent-a7651664e8cef8a98`, branch `advisor/003-registration-transaction-safety`) |

## Dependency notes

- None of the three plans depend on each other — each touches a disjoint set
  of files (001: two API routes + `src/lib/auth.ts`; 002: the ten
  `_actions.ts` files; 003: `src/lib/registrations/service.ts`). They can be
  executed in any order or in parallel.

## Findings considered and rejected

None yet — this is the first pass. The following findings from the audit were
surfaced but not selected for a plan in this round (available on request):

- CI pipeline (no `.github/workflows`) — high leverage, deferred by explicit
  user choice this round, not rejected on merits.
- Dashboard list-page pagination / `_count` instead of full-row fetch.
- Zero automated test coverage on registrations/payments (no test runner
  configured at all).
- Twin ~5,800-line event-form/page-editor files with re-diverging field
  primitives (extraction stalled after 2 shared components).
- `prisma/migrations` gitignored — no schema migration history in version
  control.
- `AGENTS.md`/`CLAUDE.md`/`docs/`/`scripts/`/TODO-ISSUES-BUG docs all
  gitignored — flagged as possibly intentional; confirm with the user before
  planning.
- `better-auth` 14 patch releases behind; `@react-email/components`
  deprecated upstream.
- Two overlapping icon libraries (`lucide-react` vs `@hugeicons/react`).
- Silent error swallowing (no logging) in every dashboard server action catch
  block.
- Unauthenticated, unrated-limited email-enumeration endpoint
  (`registration-email-check`).
- Status/type fields modeled as plain `String` in Prisma with 4+ independently
  hand-duplicated TS unions (one already inconsistent with another).
- Missing `@@index` on hot foreign-key columns (`Registration.eventId`,
  `AgendaSession.eventId`, etc.).
- N+1 sequential upserts when saving media translations.
- Unbounded main-thread image/video re-encoding on upload (no downscale cap,
  real-time video re-encode blocks for the full clip duration).
- `.env.example` missing ~10 environment variables the code actually reads.
- No pre-commit hooks; no `.editorconfig`.

Run `/improve` again (or `/improve <finding>`) to turn any of these into a
plan.
